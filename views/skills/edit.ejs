<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= skill.skill %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />

</head>
<body>
    <h1>New Skill</h1>
    <form action="/skills/<%= idx %>?_method=PUT" method="POST" autocomplete="off">
        <input type="checkbox" name="learned" <%= skill.learned ? 'checked' : '' %>>&nbsp;&nbsp;
        <input type="text" name="skill" value="<%= skill.skill %> ">
        <button type="submit">Change Skill</button>
    </form>
    <p>Check the box if you have learned <%= skill.skill %> </p>
</body>
</html>